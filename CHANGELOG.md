# 更新日志

本文件记录不白记（原 Veri Fin）面向用户的版本变化，最新版本在前。仅收录用户可见的改动，纯内部重构 / 文档更新不在此列。

## [Unreleased]

### 变更
- 首页概览移除「日均消费」小卡片，改为在趋势图中用黄色水平线展示本月日均消费。

### 修复
- 交易行账户名称固定靠右显示，长账户名在右侧列内省略，不再挤占日期和标签。
- 修复 WebDAV「自动同步」与「立即同步」未真正完成跨设备增量上传、下载和本地合并的问题；同步事件、待上传队列和失败状态现在可持久化，应用回到前台或手动触发后会继续处理。同步状态只有在实际成功过且没有待处理项时才显示「已连接」，首次同步、运行中、错误、待同步和冲突会分别显示真实状态。

## [1.19.2] - 2026-09-16

### 变更
- 应用名称改为「不白记」，同步桌面与分享入口名称、引导、设置、默认昵称、解锁提示及应用内品牌文案；保留原包名与数据标识，已有账目和自定义资料不受影响。
- 应用图标更新为青蓝圆章与金色边框内戴红围巾的白兔，并同步 Android 桌面图标与项目品牌素材。
- 首页概览的「本月支出」主数字缩小到 14sp，与普通指标保持清晰但克制的层级。
- 预算设置新增按月／按年切换：可设置当前自然年的总预算和分类预算，月度执行按年度金额的十二分之一计算，已设置的单月覆盖仍优先。
- 设置的「外观」新增四档字体大小，保留系统无障碍文字缩放并在其基础上调整。
- 交易列表改为两行信息布局：首行展示末级分类、备注和金额，次行展示日期时间、标签和账户名称；转账显示转出与转入账户。

## [1.19.1] - 2026-09-15

### 修复
- 修正版本号，与 1.19.0 功能相同。

## [1.19.0] - 2026-09-15

### 新增
- WebDAV 自动双向同步：数据管理页 WebDAV 设置下方新增「自动同步」开关，与「自动备份」互斥（只能开启一个）；开启后应用前台时自动增量同步账本数据，多设备修改同一交易时检测冲突并进入「同步冲突」页手动决议（保留本地/保留远端/保留删除）；同步为最终前台一致性，只在打开应用或回前台时同步，无后台推送或轮询；手动「立即同步」始终可用。

## [1.18.5] - 2026-09-14

### 优化
- 小组件应用内预览直接使用 Android 桌面布局渲染，系统预览图也从同一布局导出；统一实色表面、字体、圆角与按钮，快速记账卡片收紧为 72dp 高。
- 修正资产曲线的数据来源，使用净资产历史而不是支出序列；预算环随金额一起处理跨期状态。

## [1.18.4] - 2026-09-14

### 修复
- 桌面小组件保留快速记账、预算、资产总额和收支趋势四个固定模板；移除用户自定义 Provider 和添加时的配置页面，直接加载固定布局与现有数据。

## [1.18.3] - 2026-09-14

### 修复
- 修复桌面小组件无法添加或添加后空白的问题：补齐用户设计组件的 Android Provider 声明、配置入口和原生数据同步。

## [1.18.2] - 2026-09-13

### 新增
- 设置 → 记账新增数字键盘布局选项，可在当前标准顺序（7 8 9 / 4 5 6 / 1 2 3）与电话顺序（1 2 3 / 4 5 6 / 7 8 9）之间切换（Issue [#41](https://github.com/LumiDesk/verifin/issues/41)）。
- 锚点菜单支持根菜单和子菜单按内容自适应宽度，减少短选项周围的空白。

## [1.18.1] - 2026-09-13

### 变更
- 小组件改为固定模板和桌面实例配置：应用内仅展示模板，账本、指标、图表范围、背景与点击动作随桌面小组件保存，删除小组件时同步清理且不参与备份导出。

### 修复
- 对齐系统小组件选择器中的模板尺寸：快速记账 1×2、预算与资产 2×2、收支趋势 2×4，并关闭自由缩放。

### 修复
- 修复 Android 小组件选择器仍显示旧固定入口的问题，统一为 VeriFin 自定义入口并在系统预览中展示实际组件布局；添加失败时显示明确的手动添加指引。

## [1.18.0] - 2026-09-13

### 新增
- 重构桌面小组件中心：新增收支趋势组件，并支持选择组件模板、账本、主/辅助指标、图表范围、点击动作和金额隐私显示。

### 修复
- 小组件画布移除重复标题，空态改为指向右上角加号；2×2 预览恢复正方形，统一文字左边距与记账按钮对齐。
- 修复长按管理时组件递归构建和创建页开关绘制异常；预览金额、预算比例和趋势使用所选账本的真实数据，取消选择不再清空原配置。
- 调整小组件画布编辑态：长按直接进入拖动与删除状态，仅当前组件显示外置红色删除入口，空白区域可退出，拖动反馈保持原尺寸并支持投放到空白列。
- 优化小组件拖动反馈：长按触发触感、编辑态持续抖动，原位和目标位置显示虚线占位框，删除按钮缩小并适配深浅色边框。
- 修复小组件松手时先回原位再移动的问题，拖动落点保持到排序保存完成；移除按钮改用内置减号并随选中卡片一起抖动。
- 优化预算卡片与宽卡布局：2×2 预算环放大；2×4 没有辅助指标时主指标下移到左下角。
- 放大 2×4 预算卡右侧的进度环，提升宽卡中的视觉层级。
- 同步放大预算环内的使用率文字，让百分比与圆环比例一致。
- 修复用户小组件在 Android 桌面的加载与绑定，按各自账本同步真实金额和趋势；补齐冷启动点击路由及独立记账入口。

## [1.17.8] - 2026-09-11

### 修复
- 统一分类管理页的层级展开布局：移除左侧空槽，将展开箭头放到行尾并与预算分类列表保持一致。

## [1.17.7] - 2026-09-11

### 修复
- 优化季度统计图日期标签密度，修复转账统计图表与明细显示为 0。
- 统一首页、预算、看板、资产和“我的”卡片的内边距与图表纵轴留白，修正日历标题对齐、历史对比卡片间距和数字键盘底部安全区。
- 收紧首页与预算趋势图的右侧绘图区，让曲线末端与卡片内容边界对齐。
- 收紧预算主卡顶部留白，并将预算状态提示图标与文字垂直居中。
- 重做预算分类行的展开控件：顶级分类不再预留左侧空槽，展开/收起统一放到有子分类的行尾，整行仍用于编辑预算。
- 移除资产净值卡片内会误导用户进入设置页的图片按钮；资产分区折叠状态按账本持久化，分类管理默认折叠父分类。

## [1.17.6] - 2026-09-11

### 修复
- 设置里「单币种隐藏单位」的说明写全了：此前只提「不在每个金额旁重复显示单位」，但它实际还管页头、卡片角标和 AI 助手回答里的币种。
- AI 信用卡表格在显示单位时补上币种列，多币种信用账户的欠款、额度和账单金额不再混淆。
- 底栏在页面尚未挂载时保留点击目标，下一帧继续切换；导航文字恢复为 12sp。
- 无参数的旧布局对照构建也使用纯色页面背景，与当前材质规范一致。

## [1.17.5] - 2026-09-10

### 修复
- 「单币种隐藏单位」现在处处生效。此前账本只有一种货币时，首页、资产、看板、预算、预算历史、预算设置、统计分析、交易明细、账户详情、账户报告等页面的标题下方，以及首页的预算卡、日历卡和 AI 财务助理的结果卡上，仍然会显示「单位：¥」或「本位币 ¥」。这些位置当时各自拼接文字，绕过了开关；现在统一走同一个判断。多币种账本照旧显示单位，不会误隐藏。
- 交易列表里同币种转账的副行不再重复两端金额。此前写作「100 ¥ → 100 ¥」，而同一行主金额已经是「100」；跨币种转账仍保留两端单位以分辨币种。
- 打开「显示逐笔结余」后，每行的余额不再重复带单位（「余额 970」而不是「余额 970 ¥」）。
- 退款面板里的「账户实际到账」「本位币冲抵额」不再重复带单位，与同屏顶部的退款金额写法一致。
- AI 财务助理的步骤摘要不再给每个金额都写上币种代码；单币种账本下，助手回答正文里也不会再出现「合计 CNY 4,300」这类与结果卡片不一致的写法。
- 「我的」里的「货币与汇率」入口在账本只有一种货币时不再显示币种代码；账本列表的行尾只在存在多个账本时显示币种，用来区分各账本的口径。
- 首页日历卡在隐藏单位后不再于底部空出一条：此前那句「单位：¥」不显示了，但为它留出的那点间距还在，卡片底部看起来空了一块。

## [1.17.4] - 2026-09-10

### 变更
- 切页动画改成弹簧：底部导航的页面过渡不再按固定时长播放，而是像真实滚动一样按「当前位置 + 当前速度 + 目标」平滑收敛（感知时长约 250ms、无回弹）。此前是固定时长的曲线插值，每次点击都要从头重新计时，连点越快、每次都只能走到起步阶段——实测每 16ms 点一次时页面完全不动，看起来像卡死。现在连点时页面会跟着连续地来回滚动，松手后平滑停在最后点选的那个 Tab。

## [1.17.3] - 2026-09-10

### 修复
- 底部导航的切换动效不再被打断：此前只要页面重建一次（切页过程中会发生两三次），导航里的四个图标就会被整体重置一遍，表现为图标毫无理由地重新抖一下；快速左右滑页或连续点不同的 Tab 时，选中动效会直接消失。
- 图标选中时的摆动回到设计意图：此前在动画的头尾会瞬间旋转好几圈、看着像卡了一下；被取消选中那个图标的旋转方向此前也是恒定的，不随切换方向变。现在从 0 摆到最明显再回到 0，方向跟随切换方向。
- 修掉一个「点了没反应」的卡死：快速连续点底部导航时，某一次点击可能被悄悄吞掉，界面停在上一个 Tab 上不动。原因是切页动画在滚动位置尚未就绪时会一步都不走就返回，而收尾逻辑接着把导航下标改回页面实际所在的页——等于这次点击没发生过。现在检测到「动画结束但页面没到位」会重新切一次，仍不成功就直接落位，保证点了就一定到。
- 在非首页的 Tab 上按安卓返回键回首页，以及跨多个 Tab 的直接点按，都照常播放页面滚动动画（跨多页不做瞬移）。
- 切页动画还没播完就点别的 Tab 时不再「顿一下」：此前每次都按同一条曲线从 0 重新插值，起步速度接近 0，实测改点目标的那一帧每帧位移会掉到原来的三成。现在按当前滚动速度接上一条起点斜率匹配的曲线，速度不断档，时长与落点不变。

### 变更
- 「理财/统计/记账」等页面里那种滑动选中的分段控件，选中胶囊不再偏窄。此前胶囊宽度是写死的：选项为 4 个时（总宽已超出）看起来正常，2 个和 3 个的都明显偏窄——2 个选项时胶囊只盖住约三分之一的格子。现在胶囊宽度等于轨道总长除以选项个数，正好铺满一格。
- 记账页类型切换里的「转账」选中色改用蓝色，与下方的大金额、分类图标一致（此前是中性色，深色模式下接近全白）。分类管理与周期记账页的同一个控件也一并统一。

## [1.17.2] - 2026-09-10

### 修复
- 底部导航在关闭系统手势提示线的机器上不再贴住屏幕下边缘：系统不报底部留白时保留最小间距，同时仍尊重真实的三键导航/手势条留白（取较大值，不叠加）；键盘弹出时底栏也不再跟着跳动。
- 点击底部导航后，页面切换不再比底栏动画晚约 200ms 才起步：两者现在同时开始、同时结束。
- 底栏切换动效中飞过的圆点改用品牌色（此前是库默认的蓝配红，与整体配色不符）。

## [1.17.1] - 2026-09-10

### 变更
- 移除磨砂玻璃与「高级材质」：卡片、底部导航、快捷记账按钮、菜单和弹层改为不透明实色表面，去掉背景模糊、方向高光与导航折射；设置里的「高级材质」开关一并移除。外观更干净直接，滚动与拖动也不再需要每帧做背景模糊。
- 移除全局背景渐变光效，页面背景改为单一纯色。
- 统一全部「几选一」切换条：记账页的支出/收入/转账、统计分析的区间与口径、账户详情的日/月、首页收支统计的周/月/季/年、分类与周期记账的类型、以及排行分组，现在共用同一个滑块式分段控件，选中项用滑动胶囊指示。此前这些控件至少有六种不同的轨道色、圆角和选中样式。
- 浮动元件（底部导航、快捷记账按钮、菜单）改用比卡片更亮一档的表面色。此前它们与页面背景几乎同色，深色下导航胶囊看起来像一块没有边框的凹陷。现在靠表面色差、描边和阴影表达层级。
- 底部导航改为**停靠式底栏**：整宽贴底、不透明，条目由 `bottom_bar_matu` 绘制，选中时有双圆点动效；四个中文标签常显。底栏底色一直铺到屏幕最底（含系统手势条背后），不再出现「手势条区域颜色和底栏不一致」的分块。不再使用浮动胶囊，也不再支持拖动切页。
- 记账按钮改回**右下角浮动圆角方形**（品牌蓝 + 白色加号），压在内容之上，只在首页显示，进出带缩放淡入淡出；长按仍走 AI 记账。
- 底栏图标未选中为线框、选中为填充，切换时最直观。
- 入门引导更新：图标底板改为品牌色的柔和渐变并放大，页码指示的当前项拉长为胶囊，排版更舒展。

### 修复
- 看板与统计分析页的分区标题右侧文字（如「分类统计」右边的「-45 · 9月 · 支出」）之前停在卡片中间，现在按设计贴到卡片右端。
- 记账页的转账选中色改用语义色路径，不再硬编码品牌蓝，与支出/收入的处理保持一致。

## [1.17.0] - 2026-09-09

### 优化
- 设置里新增「显示逐笔结余」开关（默认关闭）：打开后交易列表每行显示该账户在这笔交易后的余额。
- AI 财务助理的结果卡片（标题、指标名、表头、提示语）跟随应用语言显示中英文，不再固定中文。
- 图表会向屏幕阅读器播报整图摘要（折线图 / 柱状图各有多少个数据点、可点按或滑动查看），不再是一个无标签的图形。
- 开启高级材质时，拖动导航不再每帧重建整条轮廓的采样点，拖动更跟手、更省电。
- AI 财务助理新增六类只读查询：收支趋势、环比同比、账户余额一览、净资产、信用卡账单、预算执行；缺汇率时明确说明缺哪种币，不给部分和。
- 新建信用卡 / 信用账户时可以直接填写信用额度、账单日与还款日，不必建完再进详情页补一遍。
- 切回已经访问过的页面不再整页重建：四个根页面保持存活，来回切页更跟手、更省电。
- 长按交易图片缩略图即可删除附件，不必只能点右上角的小叉。
- 浅色模式下收入、支出、转账与提醒的金额和图标改用更深的同色系颜色，对比度从最低 1.78:1 提升到 4.5:1 以上，弱视与强光下更易辨认；深色模式外观不变。
- 设置页的开关整行都可以点击，不再只能点右侧那个被缩小的开关。
- 分类胶囊右下角的「更多」圆点扩大了点击范围，更容易点中。
- 错误提示的停留时间从 4 秒延长到 8 秒、正文最多显示六行，并让屏幕阅读器主动播报。
- AI 结果的表格列多或文字长时改为在卡片内横向滚动，不再撑破卡片。
- AI 设置页说明 AI 是可选功能，不配置也能正常记账。
- 导入预览标题显示「导入到「账本名」」，多账本时不再担心存错地方。
- 切换账本后给一条轻提示，确认确实切过去了。
- 还款遇到缺汇率时，页面直接说明可以手填银行实际扣款金额，并给出去「货币与汇率」的入口。
- 资产页还没有账户时，空状态里直接提供「添加第一个账户」按钮。

### 修复
- 导入预览把内容全部排除时，底部按钮改为说明「没有勾选任何可导入的内容」，不再显示「只导入 0 个账户」。
- 退款编辑弹层内容超过一屏时可以滚动，矮屏上不再够不到保存按钮。
- 记账页大金额的负号改为与其他金额一致的写法（原先单独用了 U+2212 加空格）。
- 交易列表搜索「退款」能命中关联了报销/退款的交易，且不会误命中其他交易。
- 记账提醒的「发送测试通知」在通知权限被拒时不再谎报「已发送」。
- 周期记账补记失败时提示「补记失败，请重试」，不再显示「保存失败」。
- 净资产卡的资产 / 负债行在系统字号放大后不再被挤出卡片。
- 系统字号放大后，锚点菜单的行高不再错位。

## [1.16.16] - 2026-09-09

### 优化
- 系统字体放大后不再裁切或溢出：「我的」宫格按最长标签自动减少列数（中文常规字号仍是四列）、标签允许两行，预算指标块、日历日期格和页面标题按字号长高，首页摘要胶囊、底部导航文字和小节标题在放不下时改为省略而不是撑破布局。
- 金额键盘的数字显示行与按键区各自独立成层，按数字时不再连带整块弹窗背景重算模糊。
- 交易列表翻到深处时，不再被后台通知（周期补记、改设置等）打回最前面 30 条并跳回顶部；只有自己改筛选条件才回到第一批。
- 自动识别开启时，记账页会按所选分类上次用过的账户预选；关闭自动识别时仍只用账本默认账户。

### 修复
- 修复开启高级材质后按住底部导航时图标和文字上下颠倒的问题（透镜采样多做了一次 Y 轴翻转；OpenGLES 后端受影响，Vulkan 不受影响）。
- 报销状态统一为「待报销 / 已到账」：交易行徽标、筛选、首页指标与退款清单用同一套说法；只报回来一部分的支出不再同时出现在「待报销」和「已报销」两个筛选里。
- 保存按钮不可用时说明原因（缺金额、缺账户、转账未选转入账户、退款超过原金额），不再只显示一个灰按钮。

## [1.16.15] - 2026-09-09

### 优化
- 降低日常操作的开销：底部导航拖动与切页不再连带重绘整屏，交易列表搜索改为停顿后统一筛选，首页与资产页的余额、日历和月度走势改为单遍计算，保存单笔交易不再整表重写。
- 图表纵轴刻度与网格线对齐、空数据不再画出一条假平线，图表文字跟随系统字号并补充无障碍描述；日期统一按当前语言显示（如「8月12日」「2020年1月」），金额零值不再出现带符号的 `+0` / `-0`。

### 修复
- 首启动引导（含「跳过」）必定创建一个默认账户，修复零账户时第一次记账保存按钮永远禁用的问题；记账页无可用账户时提供「添加账户」入口。
- 信用卡还款页未改动内容时返回不再弹出「未保存」确认，避免误点主按钮「保存」直接写入一笔还款。
- 应用锁锁屏页新增「忘记密码」入口：两步破坏性确认后清空本地数据并关闭应用锁，替代原先指向锁屏之外设置页的无效指引。
- 交易列表新增记账按钮，「待退款」入口不再在无待办时消失；只按时间筛选时显示「没有匹配交易」，清空筛选会一并复位时间。
- 看板顶部新增本月收入 / 支出 / 结余摘要，四张无数据时会显示空态的图表补齐空态，预算执行卡可直接进入预算页；「月度收支」面板更名为「月度趋势」，与其实际只展示支出序列一致。
- 「我的」新增「预算」与「AI 财务助理」入口，避免首页预算面板被关闭后预算功能不可达、AI 助手无处可进。
- 隐私政策补充「AI 功能与第三方服务」一节，并修正「网络」仅用于检查更新的旧表述；AI 聊天页标明对话只存本机、不含在备份内。
- 资产页缺汇率提示卡可直接点进「货币与汇率」维护；术语与口径统一：资产卡显示「总资产 / 负债」，看板副标题改为「预算与统计」，待报销标记补充「标记本身不产生资金变动」说明。
- 数据安全：清除备份目录改为需确认；导入全部失败时标题改为「没有导入任何交易」，并显示具体失败原因。
- 对话框按钮点击区域放大到至少 48×44，降低误触。

## [1.16.14] - 2026-09-08

### 修复
- 数字键盘顶部间距与左右统一为 14，拖拽把手内嵌到玻璃面板内不再悬空。
- 选择类弹窗标题顶部间距与左右统一。

## [1.16.13] - 2026-09-08

### 修复
- 快速记账金额键盘恢复顶部间距，隐藏标题后数字区域不再贴住弹窗顶部。
- 统一 AI 记账与退款编辑弹窗的顶部间距，与其他弹窗保持一致。
- 无账户转账（代还）转入外币账户时金额币种跟随转入账户，避免保存失败；无账户转账不再显示手续费。
- 修复记账页保存按钮禁用态仍显示主题灰而非淡蓝的问题。
- 底部弹窗底部两角改为直角，只圆顶部两个角。

## [1.16.12] - 2026-09-06

### 修复
- 保存栏背景改为透明，数字键盘顶部间距与左右边距统一，并保留弹窗通用顶部间距。

## [1.16.11] - 2026-09-06

## [1.16.10] - 2026-09-06

### 修复
- 修复 Android 英文资源中的 AAPT 编译错误。

## [1.16.9] - 2026-09-06

### 修复
- 恢复统一设计发布包首页最近交易显示 5 条，修正无账户代还交易详情的展示与编辑能力。
- 快速记账金额键盘不再保留隐藏标题的空白占位；桌面小组件过期数据改为提示刷新，并补齐系统入口的中英文资源。
- 备份归档保留附件 MIME 类型并增加读取/解包大小保护。

## [1.16.6] - 2026-09-06

### 修复
- 修正快速记账隐藏标题后数字区域上移的问题，保留原来的顶部间距和布局。

## [1.16.5] - 2026-09-06

### 修复
- 快速记账金额键盘不再重复显示“快速记账”标题，保留其他金额输入场景的标题与说明。

## [1.16.4] - 2026-09-06

### 修复
- 修复玻璃菜单首次出现时的透明背景闪黑：使用保留下层像素的合成方式，覆盖零进度与极小进度帧。
- 修正玻璃菜单首次出现时短暂闪黑的问题，保留平滑淡入淡出；补充零进度与极小进度的像素回归检查。

## [1.16.3] - 2026-09-05

### 修复
- 玻璃菜单的底色、模糊、光照与文字同步渐隐，修正关闭时只缩小后突然消失的问题；动画尊重系统减少动态效果设置。
- 设置按外观、金额显示、通用、记账分组，保留统一保存草稿的交互。
- 同意协议后直接进入引导，避免先露出首页；导航改为同帧过滤，移除高亮阈值变化时的快照失效切换。
- 深色输入框移除默认白边，保留聚焦和错误提示；减弱菜单遮罩和染色，恢复可见的磨砂背景。
- 按反馈撤回首页预算环的玻璃效果，恢复原来的常规渐变进度环。
- 修正玻璃页面背景的归属和过渡基色，避免进入页面、预测性返回时透出其他页面；关闭玻璃列表的越界拉伸，调整模糊合成路径。
- 最近交易标题移除按压横条。
- 恢复 v1.15.17 的全局字号与页面标题字重，保留首页主金额、导航的局部尺寸，避免材质预览放大资产等页面字体。

## [1.16.2] - 2026-09-05

### 调整
- **聚焦 Android**：移除 Web 预览工程、浏览器存储/下载适配与对应 CI 门禁，开发和界面评审转为 Android 真机；保留手机上的 WebDAV 备份功能。

### 修复
- **Android 高级材质**：将每张卡片数百次逐段模糊改为两次连续高光绘制，解决已在 REDMI K90 Pro Max 复现的 GPU 资源分配失败与闪退；恢复高级材质开关，保留方向光、动态导航折射和已保存偏好。默认仍关闭，账目数据不变。

## [1.16.1] - 2026-09-05

### 修复
- **高级材质启动恢复**：原生端暂时停用高级光照与导航透镜，旧版本已保存“开启”也会安全使用普通磨砂；隐藏原生端开关，防止再次进入高风险渲染路径。保留账目和偏好，无需卸载或清除数据；实际恢复待同签名修复包覆盖安装验收。

## [1.16.0] - 2026-09-05

### 新增
- **统一玻璃外观**：Android 发布包采用统一的页面标题、卡片圆角和间距，覆盖首页、资产、看板、我的以及记账、管理和设置页面；Web 使用同一套布局。
- **高级材质开关**：在「设置 → 通用」开启并保存后，启用方向高光和导航动态透镜；默认关闭，使用普通磨砂。偏好仅保存在设备，不随账目备份迁移。
- **真实应用 Web 开发预览**：直接运行正式页面和记账逻辑，SQLite 数据持久化到 IndexedDB，支持偏好保存、图片选择及文件导入导出；移除独立 UI Lab。

### 优化
- 首页保留概览指标方块，摘要胶囊靠右；卡片上下内边距统一为 12dp，最近交易显示三笔并保留完整列表入口。
- 预算保留原始圆环、支出、剩余日均和预算总额，圆环采用柔和同色光泽，不绘制内外白线。
- 我的页面保留四列宫格，整理标题与说明字号，改善窄屏显示；移除 Web 专属开发提示。
- 深色玻璃高光单独降低亮度；最近交易标题不再出现鼠标悬停底色。

### 修复
- 高级材质下导航静止时显示实时文字，按压时重新采样，避免首次缓存字形失真；保留拖动、吸附和取消行为。
- 应用锁显示时隐藏底层账目绘制及焦点访问，避免半透明材质透出信息，解锁后恢复页面状态。

## [1.15.17] - 2026-08-30

### 优化
- **汇率说明不再长期占用页面空间**：货币与汇率页改为从右上角问号查看本地维护、不会自动联网刷新及历史交易冻结口径。

### 修复
- **个人生日可以清除**：已设置生日时会显示明确的清除按钮，清除后仍按个人信息编辑流程保存或放弃。

## [1.15.16] - 2026-08-29

### 新增
- **账户图标新增 15 个常用平台与卡组织 Logo**：加入余额宝、云闪付、美团、国泰海通、Interactive Brokers、Visa、American Express、JCB、Diners Club、Discover、Wise、Revolut、Payoneer、Monzo 与 N26，并按支付、投资理财、卡组织、跨境与数字账户等用途重新分组；银行仍放在最后，组内优先展示常用选项。

## [1.15.15] - 2026-08-29

### 优化
- **账户图标统一为更清晰的白底 SVG**：七个通用账户图标替换为按钱包、信用、银行、现金、投资、储蓄和银行卡语义分别配色的 Phosphor 图标，品牌图标保留各自颜色；全部图标统一使用纯白底与 10% 内边距，旧图标数据会在升级或导入时自动迁移。

## [1.15.14] - 2026-08-29

### 修复
- **账户分组向下拖动可正常保存排序**：适配新排序回调已经归一化的目标索引，不再把向下移动重复减一后变成原地操作，拖动后保存按钮会正确启用。

## [1.15.13] - 2026-08-29

### 优化
- **账户分组回归纯文件夹管理**：分组不再设置自定义图标或在管理页混入资产合计；点击分组行或行尾三点会打开统一锚点菜单，删除非空分组时明确提示账户将移入“未分组”。
- **账户图标更容易查找和扩展**：图标选择器改为通用、信用、支付平台和银行分组网格，支持银行全称、简称与 ICBC/CMB 等缩写搜索；账户详情设置行也会直接预览当前图标。

## [1.15.12] - 2026-08-27

### 修复
- **恢复 Android 发布构建**：补齐应用内更新断点续传失败分支的预发布状态变量，并在普通 PR/main CI 增加 Android debug 编译门禁，避免 Kotlin 原生桥错误拖到发布标签阶段才暴露。

## [1.15.11] - 2026-08-27

### 修复
- **应用内更新支持断点续传**：APK 下载遇到锁屏断网、读取超时或连接中断时会自动尝试续传，并保留按版本隔离的已下载字节；再次检查更新可看到已保留进度并点击“继续下载”，不再退回版本号 `--` 后从零重下。完整安装包仍须通过长度、SHA-256（Release 提供时）、包名和版本校验后才会交给系统安装器。
- **快速连续操作不再让已删除交易重启后复活**：SQLite 仓储会串行执行“计算行差分 → 事务写入 → 更新快照”的完整过程；删除交易、账户、分类、标签和分组等跨表命令改为事务成功后才更新界面，落库失败不会留下半套数据。
- **批量改账户守住多币种金额语义**：只允许在原账户币种一致且已有实际账户金额时批量调整；跨币种、无账户或转入冲突会要求逐笔编辑，不再把旧账户金额直接解释成目标币种。
- **本机数据与外部图片边界加固**：Android 明确禁用系统 Auto Backup，避免账目和本机凭据未经应用内操作进入系统云备份；其他 App 分享的图片改为边读边限制 25 MB，超限立即停止，避免完整读入后才检查导致内存耗尽。应用锁与隐私同意等关键设置也会在持久化成功后才生效。

## [1.15.10] - 2026-08-25

### 修复
- **多币种退款与历史金额口径加固**：外币账单导入会按原币、到账账户币种和本位币分别生成正确的关联退款；编辑历史交易日期不再静默重算冻结金额，报销筛选改用本位币比较，退款只在实际到账日改变余额趋势，已有退款的交易会明确锁定原币。
- **非两位币种金额精度统一**：首页、预算、看板、统计和图表的金额格式与零值判断会跟随当前账本本位币；快速记账、预算、余额调整和信用额度输入会按 JPY/KWD 等币种的实际小数位限制，并在保存预算时再次规整。
- **混币比较不再裸比数字**：交易列表按金额排序会统一换算到本位币，自动识别只在同原币记录间比较金额；AI 查询转账时会展示两端真实金额，金额筛选和排序不再把转账当成 0。

### 优化
- **汇率使用情况与待处理状态更透明**：外币录入和历史交易会显示实际采用的汇率生效日与过期提示，资产页标明估值使用的最旧汇率；周期规则缺率时首页直接提示，补齐汇率后可从轻提示立即补记。
- **桌面小组件随财务状态及时刷新**：汇率、预算、账户、账本切换和历史交易编辑等变化会统一触发去抖刷新，不必等待重新打开应用或再记一笔才更新资产与预算数据。
- **外币编辑与导入失败更可解释**：手工结算、导入和固定周期规则修改原币金额时会保持既有结算比例；交易保存、账单导入和备份恢复增加共享三层金额校验，校验失败会明确提示且不写入半套数据。

## [1.15.9] - 2026-08-25

### 修复
- **轻提示长文案不再被单行截断**：提示尺寸和正文排版会根据内容与屏幕宽度自适应，整体字号与触控区域略微放大，分类删除限制等较长信息可自动换行完整显示。

## [1.15.8] - 2026-08-25

### 优化
- **应用内轻提示更紧凑且可跨页面保持**：新增根级轻提示组件，支持多条堆叠、独立关闭、倒计时进度、常驻、操作按钮与重复合并；“再次返回退出”和全局保存失败已改用新提示，切换 Tab 或进入详情页时不会消失。

## [1.15.7] - 2026-08-25

### 修复
- **快速记账真机布局与分类颜色调整**：底部保存按钮增加下方留白并收窄左右宽度；分类图标明确按支出、收入、转账显示红、绿、蓝，在深色模式下不再继承为白色。

## [1.15.6] - 2026-08-25

### 优化
- **快速记账页更轻、更适合单手操作**：保存操作移到底部固定按钮；支出/收入/转账切换更紧凑，常用分类改为三列胶囊，子分类通过右下角省略号进入原有多级选择器；日期、时间、标签、待报销、附件和币种收进“更多信息”，标签仍可多选/新建，多张附件以横向缩略图展示并可逐张删除。

## [1.15.5] - 2026-08-25

### 修复
- **修复浮动导航导致根页面卡片异常增高**：隔离 `extendBody` 注入的底栏安全距离，首页日历和“我的”功能宫格不再自动继承整段底栏高度；根列表末项仍会正确避让浮动导航。
- **拖动导航松手后从手指位置继续吸附**：程序化跨页时不再把 PageView 途经的中间页误当成新选择，滑块不会向原 Tab 回退后再重新移动；直接左右滑动页面仍会同步导航。

## [1.15.4] - 2026-08-25

### 修复
- **浮动导航不再残留整宽旧底栏背景**：根页面内容现在延伸到玻璃导航背后，并将导航在系统安全区之外的左右、底部留白统一增加到 24dp，避免真机上贴边。

## [1.15.3] - 2026-08-25

### 优化
- **底部导航升级为可拖动的浮动玻璃胶囊**：四个根页面使用无渐变、无固定染色的中性玻璃导航；按住滑块可连续拖动并柔顺吸附，Hover 不再叠加矩形背景。快捷记账改为同材质圆形按钮且仅首页显示，点击/长按行为保持不变，并针对 360dp 窄屏自动收窄。

## [1.15.2] - 2026-08-24

### 修复
- **重复下载更新不再产生损坏安装包**：更新下载期间无法再通过点击弹窗外或系统返回键关闭并重复发起下载；Dart 与 Android 原生层都会复用或拦截并发任务。安装包先写入隔离临时目录，完成长度、包名和版本校验后才提交；关闭更新弹窗后再次检查会直接恢复“立即安装”，不再重复下载同一版本。

## [1.15.1] - 2026-08-24

### 优化
- **更多短选择改为就地展开**：主题、语言、金额单位、记账按钮行为、自动备份、AI 工具协议、周期规则、交易筛选与排序、收支统计、账户/交易类型、性别、曲线数据和附件来源现在都贴近触发位置打开；账户类型会说明可用能力，编辑时来回切换不会丢失暂时隐藏的卡号与信用草稿，已有退款的交易则保持类型锁定。

## [1.15.0] - 2026-08-24

### 优化
- **常用操作改为就地展开的多级菜单**：资产页、账本、分类、标签和汇率历史的操作菜单现在贴近触发位置打开，支持图标、副标题、分组、选中/禁用状态；分类编辑与资产背景预设可在保留上一级卡片的情况下平滑递进，资产显示设置仍须点击保存后才会生效。

## [1.14.1] - 2026-08-18

### 新增
- **货币单位显示可自定义**：「我的 → 设置 → 金额显示」可在「符号后置（100 ¥）」与「代码前置（CNY 100）」之间切换，并可选择单币种账本是否隐藏重复单位；默认使用更简洁的符号样式且在单币种账本隐藏。首页概览、预算、日历、看板和统计页使用轻量「单位」提示，避免每个数字都重复堆叠 CNY/USD。

### 优化
- **汇率更易读**：转账与换算页不再显示 `0.138888889` 一类过长小数，常见汇率最多显示 4 位小数，极小汇率自适应保留 6–8 位；保存和 CSV 导出仍保留原有高精度。

### 修复
- **外币收支统计口径统一**：收支统计详情的总额与按日/按月明细现在和图表一样使用交易保存时冻结的本位币金额，不再直接相加不同原币数值。

## [1.14.0] - 2026-08-18

### 新增
- **离线多币种账本与本地汇率管理**：账本可选择 ISO 4217 法定货币作为本位币，账户可独立选择币种；「我的 → 数据与工具」新增「货币与汇率」，支持按日期手动维护本机汇率和查看历史。旧账本首次使用时可确认原有金额的真实币种，整个过程只重解释币种标签、不联网也不改动数值。
- **跨币种交易、转账、退款与周期记账**：收支会保留原币、账户真实扣入账金额和冻结本位币金额；跨币转账记录两端实际金额，退款支持跨币到账，周期规则可使用到期日有效汇率或固定金额。缺少必要汇率时明确等待补齐，不按 1:1 猜测。
- **多币种资产、统计与导入导出**：账户显示原币余额，总资产按本地汇率完整折算，缺率时不再显示误导性的部分总额；预算、首页、看板、AI 和桌面小组件统一标明本位币。本应用 CSV 新增多币种金额/账户币种列和交易导出，JSON v2 备份完整保留汇率与三层金额并兼容旧备份。

### 优化
- **金额精度按货币标准显示**：JPY 等 0 位、CNY/USD 等 2 位、KWD 等 3 位；原「强制两位小数」设置平滑迁移为「标准精度 / 精简尾零」。

## [1.13.1] - 2026-08-11

### 修复
- **零余额显示语义统一**：账户余额按分规整，消除长期流水累加产生的浮点残差；资产页零余额改用主题中性色，与正余额、负余额及不计入资产的弱化账户明确区分。

## [1.13.0] - 2026-08-06

### 优化
- **编辑与保存操作全应用统一**：账户、预算、设置、提醒、面板、周期规则、资产显示及管理排序等页面现在都先编辑本地草稿，右上角使用明确的软碟保存按钮；有修改时返回会询问“保存 / 不保存 / 取消”。资产视图、背景和排序集中到「资产显示设置」，普通分区展开/折叠不再悄悄持久化。

### 修复
- **交易退款状态保存后立即正确**：交易详情现在把交易字段、退款和附件作为一个整体保存，已到账退款会在提交时重新计算并与附件原子落库，不再被进入页面时的旧数据覆盖；报销徽标、筛选、首页指标和净支出无需重启即可正确更新（issue #31）。

## [1.12.0] - 2026-08-06

### 新增
- **AI 账目问答升级为只读 Agent**：Agent 会自主选择并调用当前账本的查询工具，聊天中可展开查看「正在调用 / 已完成 / 失败 / 重试」步骤；AI 设置新增自动、原生 Tool Calls、兼容模式三种协议选项，并可检测当前端点的普通补全、流式响应和工具调用能力。

### 修复
- **AI 工具 JSON 与连接异常不再直接显示**：原生 Tool Calls 与兼容标记协议现在共享结构化状态机，工具参数、推理文本和底层网络异常不会冒充最终答复；SSE 中断会校验结束状态并在安全范围内至多重试一次，端点明确不支持原生工具时自动降级到兼容模式（issue #32）。

## [1.11.6] - 2026-08-06

### 修复
- **分类单期预算恢复可编辑**：v1.11.1 前设置的分类月预算虽然仍显示金额，但预算总览失去了修改入口，设置页修改默认预算也会被旧单期值继续覆盖；现在可直接点击预算总览的分类行，修改该月/该期额度，或清除单期设置并恢复沿用默认预算（issue #28）。

## [1.11.5] - 2026-07-27

### 修复
- **夏令时地区的日期计算偏差**：在实行夏令时的地区（欧美等），换季当天只有 23 小时或长达 25 小时，应用此前按「固定 24 小时」做日期计算，导致跨过换季日时出现一系列偏差——统计的自定义时间范围少算一天（连带日均偏高）、每日记账提醒会漂移一小时（设 21:00 却在 22:00 响）、换季日次日把前一天的交易标成「今天」、信用卡还款剩余天数少一天，「本周 / 近 7 天 / 近 30 天 / 近 6 周」等时间筛选与前后翻页也会错位一天。现已全部改为按日历日计算，任何时区、任何季节结果都一致。中国及其他不实行夏令时的地区不受此问题影响，行为无变化。

### 修复
- **输入特定金额记账时白屏**：记过退款的用户，在记一笔时输入与某笔退款金额相近的数字（如 4.85、5），点确认后会直接白屏、无法记账（issue #26）。原因是「自动识别」会把历史里的退款条目当成参考、把这笔账推断成「退款」类型，而退款不是能手动记的类型、也没有对应分类，界面因此崩溃。现在自动识别只参考支出和收入的历史，退款条目完全不参与（其备注和标签也不会再被带到新账上）；同时记账页做了兜底，任何情况下都不会再因为取不到分类而白屏。
- **自动识别不再把账悄悄记成转账**：此前只要历史里有几笔金额相近的转账，新记的账就可能被自动切换成转账类型。转账不计入收支统计，这笔钱会直接从收支里消失，且不易察觉。现在转账不再被自动推断；手动选择转账后，仍会照常按转账历史带出备注和标签。

## [1.11.3] - 2026-07-26

### 新增
- **记账「自动识别」可关闭**：记一笔时应用会根据你以往的记账习惯自动填好类型、分类、标签和备注，但「字段自己被填上」对部分用户是困惑而非便利（issue #27）。现在「我的 → 设置 → 记账」新增了「自动识别」开关，可以随时关掉——关掉后新建交易一切保持默认、不再预填任何字段，已记的账不受影响。默认保持开启，行为与之前一致；该开关全局生效（不分账本）并随备份导出还原。

## [1.11.2] - 2026-07-20

### 修复
- **更新下载完成后可手动重新安装**：检查更新下载完安装包后，若在系统安装确认页误点了取消/返回，之前只能重新下载整个安装包才能再次安装；现在下载完成后弹窗的主按钮会变为「立即安装」，可反复点击重新拉起系统安装器，直接复用已下载的安装包、无需重下。

## [1.11.1] - 2026-07-18

### 新增
- **默认月预算，每月自动沿用**：新增「默认月预算」，设一次后每个月自动套用，不用再逐月重设（issue #21）；分类预算同样支持默认值。个别月份（春节、旅行等）可单独设一个不同的额度「单月覆盖」，其它月仍沿用默认，随时可一键「恢复默认」。旧版每月默认 800 的写死值已移除——未设默认时不再显示凭空的预算。

### 优化
- **预算页拆分为「预算」总览与「预算设置」两页**：原「预算设置」页其实是总览看板，现更名为「预算」并改为只读查看；右上角新增设置入口（齿轮），默认月预算、按日预算上限、预算周期起始日、分类默认预算都集中到独立的「预算设置」页配置——查看与设置分离，界面更清晰。总览页月预算下方新增状态标签，一眼看清本月是「沿用默认」还是「本月单独设置」，点按即可调整本月额度。分类预算列表默认折叠（分类多时更清爽），点父分类展开子类。

## [1.11.0] - 2026-07-17

### 新增
- **预算周期可自定义起始日**：预算页右上角新增「预算周期起始日」设置（日历图标，可选每月 1–28 日）——发薪日不是 1 号的用户可以让预算按「每月 22 日～次月 21 日」这样的周期计算（issue #19）。设置后预算页、首页/看板的预算卡片与桌面「可用预算」小组件都按该周期统计，文案同步变为「本期可用/本期支出/较上期」，标题展示周期日期范围；每个账本独立设置、随备份导出还原。不设置则一切保持自然月，与之前行为完全一致。

### 修复
- **预算页「预算周期起始日」图标与月份切换不在同一水平线**：该入口图标的字形视觉重心偏下，看起来比左侧的月份切换箭头低了一点；已换用视觉居中的日历图标，两者水平对齐。

## [1.10.32] - 2026-07-16

### 新增
- **统计分析页可从分类排行直接查看交易**：顶级分类下钻弹层底部新增「查看交易」按钮，子分类拆分行也可直接点按——都会跳到已按该分类（含其子分类）预筛的交易列表，与分类管理的「查看交易」同一路径。在统计里发现「未分类」等异常聚集时，可一键定位到这批交易，配合多选批量改分类一次性归类。

### 修复
- **微信/支付宝账单导入不再产生「已删除的分类」**：微信账单此前完全丢弃「交易类型」列，导入的交易全部落成空分类，在列表和统计里显示为「已删除的分类」、也无法筛选，导入预览页也因此看不到分类映射区（issue #16）。现在「交易类型」（商户消费、微信红包等）会作为分类候选出现在导入预览的「导入分类」映射区，可整体映射到自己的分类体系或改名保留。
- **任何来源缺失分类的账单统一兜底到「未分类」**：支付宝等来源分类列为空的行此前同样落成空分类；现在统一归入「未分类」，并作为映射行出现在导入预览（有该行时分类映射区默认展开），可一键整体映射到具体分类。
- **历史导入遗留的「已删除的分类」自动修复**：此前微信/支付宝导入落下的空分类交易会永久显示「已删除的分类」；现在打开应用时会自动把这些收支交易归入「未分类」，之后可在交易列表按「未分类」筛选、配合多选批量改分类一次性归类（issue #16）。

## [1.10.26] - 2026-07-15

### 新增
- **分类管理支持「查看交易」**：在「我的 → 分类管理」里点开任一分类，弹出的菜单新增「查看交易」——点击直接跳到交易列表页，并已预设按该分类筛选（含其子分类），省去进列表后再手动选分类的步骤。

### 优化
- **标签筛选新增「无标签 / 有标签」两项**：交易列表的标签筛选此前只有 全部标签 与逐个标签，没法一眼挑出「压根没打过标签」或「打了任意标签」的交易。现在弹窗按「快捷筛选 / 标签」两组带小标题展示——「快捷筛选」里多了 无标签（筛出所有没打任何标签的交易）和 有标签（筛出打了至少一个标签的交易），下方仍是逐个标签。
- **交易筛选新增「无需报销」项**：交易列表的报销状态筛选此前只有 全部 / 待报销 / 已报销，看不到「从没标过待报销」的那部分普通花销。现在多了「无需报销」一项——切到它就能把标了待报销的（如提前囤的、还没核销的券）单独剔除，列表顶部的支出合计也随之只统计已经真正花出去的钱，不用再自己手动去减。

### 修复
- **分类管理里大类不再恒显示「0 笔」**：此前分类管理列表每行只统计「恰好记在该分类本身」的交易，导致习惯把账都记在子分类下的用户，看到所有大类都显示 0 笔、很困惑。现在大类的笔数会连同其全部子分类一起合计——记在「餐饮 / 咖啡」下的账也会计入「餐饮」大类的笔数（与点开菜单里「查看交易」含子分类的口径一致）。
- **数据看板「日趋势」卡片日期不再挤成一团**：日期较多时，看板页的日趋势卡片此前会把每一天的日期标签全部塞进横轴、挤成一片；现在与首页走势卡片一致——天数多时只稀疏标注（1 号与每 5 天），滑动时数据气泡仍显示具体某天。
- **每日记账提醒更可靠地按时弹出**：此前提醒只在应用「冷启动」时重排一次，而每日提醒底层是「触发后自排下一天」的闹钟链，一旦被系统深度休眠、从最近任务划掉或重启打断就会断链、之后不再提醒；现在每次回到前台都会重新对齐提醒时间，断掉的链能自愈。同时修复了应用启动早期取本地时区偶发失败时会永久按 UTC 计时（如设 21:00 实际次日凌晨才触发）的问题。（若仍收不到，请在系统里给本应用加「省电 / 自启动白名单」，国产 ROM 的强制后台限制无法在应用内根治。）

## [1.10.25] - 2026-07-13

### 修复
- **导入的转账不再显示「已删除分类」**：一木（及其他来源）导入的转账此前分类为空，在交易列表里被显示成「已删除分类」、也不计入分类管理的转账分类下；现在导入的转账会像 App 内记账一样落到「转出」分类，正常显示并计入。已经导入过的历史转账会在下次打开应用时自动补齐、无需重新导入。（issue #14）
- **一木旧版本账单的子分类能导入了**：部分一木版本（如 v5.9.1）导出的账单里子分类列叫「子类」（新版叫「二级分类」），此前只认「二级分类」，导致这些账单只导入了主分类、丢了子分类；现在两种列名都识别，父子层级正常还原。（issue #12）

## [1.10.24] - 2026-07-12

### 优化
- **批量改分类 / 改账户用回正规选择器**：交易列表长按多选后的「改分类」「改账户」，此前弹出的是简易列表（分类看不到层级、账户看不到余额）；现在与记账 / 编辑时一致——分类是带图标的多级层级选择器，账户带图标、余额、卡号后四位。
- **分类选择器按类型分区**：选分类的弹窗现在按 支出 / 收入 / 转账 分区并各带标题，同类内保持你在分类页设置的顺序（此前各类型分类混排，如支出的「打车」会排到转账的「还款」后面）；分类图标按类型上色（支出红 / 收入绿 / 转账蓝），与交易列表一致。分类管理页的分类图标同样按类型上色。
- **账户选择器按资产视图分区**：选账户的弹窗现在跟随资产页的视图模式——类型视图按账户类型分区、分组视图按你的账户分组（含「未分组」）分区，分区顺序与区内账户顺序都与资产页一致。
- 交易列表顶部的「账户筛选」也换成带图标、余额、分组的账户选择器（顶部保留「全部」项）。
- **更多设置纳入备份**：默认付款账户、记一笔按钮行为、金额是否两位小数、首页概览卡配置现在也随备份导出/导入，换机恢复后这些设置一并还原（此前是设备本地、换机会丢）。

## [1.10.23] - 2026-07-12

### 优化
- 新增/编辑账户时自动去除名称首尾空格；导入账单也按去空格后的名称匹配账户（「现金 」不再另建一个账户）。
- 导入账单时若已有多个同名账户，不再默认并入第一个，改为进入导入预览的「导入账户」映射区，由你决定并入哪个或保留新建。

### 修复
- 导入账单中被跳过的错误转账行（如转出与转入相同）不再在预览中产生多余的「导入标签」候选。

## [1.10.22] - 2026-07-12

### 修复
- 交易行分类标题颜色改回常规文字色（1.10.21 短暂改成了主题蓝、观感不佳）。

## [1.10.21] - 2026-07-12

### 优化
- **记账选分类更快**：有子分类的分类点一下就地展开它的子分类、直接点选，不用再打开「全部」弹窗（此前选二级分类必须进弹窗）。编辑一笔已选二级分类的交易时，对应分支会自动展开、标出当前所选。
- **交易列表显示分类层级与标签**：交易的分类现在展示完整层级（如「食品餐饮 · 午餐」，此前只显示末级「午餐」）、整条用主色；交易带的标签也会在交易行上显示（最多两个、更多收成 `+N`）。
- **「最近交易」显示日期**：首页与账户详情页的「最近交易」里，非今天的交易不再只有时间——今年的显示「07/08 14:30」、往年的显示「2024/07/08 14:30」，一眼看清是哪天（此前无论哪天都只显示时分）。
- 交易行没有备注时不再多余地重复账户名（右侧已有账户标签）。

## [1.10.19] - 2026-07-12

### 优化
- **账单导入来源选择页新增提示**：说明受各平台导出字段所限，部分账户信息可能与原软件不完全一致，可在导入预览页手动核对调整。

## [1.10.18] - 2026-07-12

### 新增
- **支持导入钱迹账单**：数据管理 → 导入账单文件里新增「钱迹」入口，导入钱迹的完整明细 CSV（「设置 → 备份与导出 → 导出账单 → CSV」）。覆盖支出、收入、转账、信用卡还款、退款、报销：退款按「关联账单」自动冲抵原支出（净额=原额−退款），二级分类还原成层级、标签一并导入，钱迹里的 0 元「心愿单」占位记录自动跳过。钱迹的「债务 / 借贷」记录（借出、收款、利息等）不导入——本应用没有债务功能、无法忠实表达（这类多是「垫钱再收回」的一进一出，跳过不影响真实收支）。

## [1.10.16] - 2026-07-12

### 优化
- **账单导入选择器更清晰**：记账软件 / 支付平台（支付宝、微信、薄荷、一木、Tally）与「CSV 模板」分成两组展示、不再混在一起。原「其他 CSV」改名为「CSV 模板」，明确只用于导入本应用下载的 CSV 模板；导入时会校验表头只能是模板列，避免把其他软件导出的 CSV 误当模板导入。

## [1.10.15] - 2026-07-12

### 修复
- **导入预览里长按交易看详情时，导入的标签不显示**（只显示「添加标签」）：草稿编辑页现在能正确解析并展示尚未落库的待新建标签，标签选择器里也能看到并勾选它们。

## [1.10.14] - 2026-07-12

### 新增
- **一木记账导入支持子分类、标签、备注**：导入一木「账单导出」时，二级分类会还原成「一级 → 二级」的父子层级（此前只保留叶子、丢了父级），逗号分隔的多标签（如「客户, 代购」）会拆分成标签并归属到对应交易，备注一并导入。导入预览页新增「导入标签」映射区，可给待新建标签改名或映射到现有标签，与账户 / 分类映射一致。（issue #11）

## [1.10.13] - 2026-07-12

### 修复
- **记账提醒收不到**：定时提醒改用**精确闹钟**，手机休眠 / 后台时也能准时触发（此前用的非精确调度在系统省电限制下常被无限推迟、根本不弹，是很多人「设了提醒却从没见过」的根因）。

### 新增
- 记账提醒设置页新增「**发送测试通知**」按钮：可当场确认通知能否正常显示；并补充说明——部分手机（小米 / 华为 / OPPO / vivo 等）需在系统里允许通知并加入省电 / 自启动白名单，提醒才不会被拦。

## [1.10.12] - 2026-07-12

### 优化
- **大数据量下更流畅、更省电**：交易多时的整体性能优化——账目列表、图表、看板在数据量大时的卡顿明显减轻。
- **交易明细列表改为分批加载**：长列表不再一次性渲染成百上千条，而是随滚动**提前无缝加载**（快到底部时就已备好下一批，不用停在底部等待）；顶部的收支合计 / 笔数仍按完整数据统计，不受影响。
- **记账写入更轻**：新增 / 修改 / 删除单笔交易时只改动那一笔对应的数据，不再每次都重写整张账目表——写入更快、更省存储擦写（对交易量大的用户尤其明显）。
- **安装包体积减小**：安装包改为 arm64-v8a 单架构（覆盖 2019 年后绝大多数机型）并开启代码 / 资源裁剪，下载与占用空间明显减小。

## [1.10.11] - 2026-07-11

### 修复
- 修复**一木记账账单导入**：账单里带「退款」的记录此前会报「金额无效」导致整单导入失败（全额退款尤其明显），且部分退款的金额会被算错。原因是一木导出的「金额」列是**已扣退款与优惠的净额**，现已正确还原为原始应付金额（优惠不计入支出、退款如实记入）。一木里的空记录（0 元、无退款无优惠）现直接忽略，不再报错（issue #10）。

## [1.10.10] - 2026-07-11

### 修复
- AI 助手页面顶部标题栏与其它页（统计分析 / 收支统计等）**左右、上下位置对齐**，此前贴边、对不上。

## [1.10.9] - 2026-07-11

### 新增
- AI 记账设置页支持**清空配置**：右上角「清空配置」按钮，确认后清空请求地址 / API Key / 模型（此前填了配置后无法一键清空）。

### 优化
- **AI 助手页面整体翻新**：头部改用与其它页一致的通用样式；输入框和发送按钮加大、发送按钮改为圆形并在无输入时置灰不可点；聊天区左右间距、消息字号与行距、图表坐标轴与表格样式全面打磨，更精致。

### 修复
- AI 助手退出再进入后，之前对话里的**图表 / 结果卡片丢失**——现在结果卡片会随聊天记录一起保存，重开时连同图表一并还原。

## [1.10.8] - 2026-07-11

### 新增
- **左右滑动切换主页面**：首页 / 资产 / 看板 / 我的 四个 Tab 之间可左右滑动切换，不必只点底部按钮。在图表或可滑动删除的交易行上拖动仍是原来的交互（选数据点 / 滑动删除），只有在空白区滑动才切换页面。
- **AI 对话查询账目**：看板页右上角新增「问 AI」入口，进入全屏聊天页，用自然语言问账目（如「这个月花最多的是哪些分类」「最近三个月有哪些大额支出」「本月收支怎么样」）。AI 会调用只读工具查询你**当前账本**的真实数据，以柱状图 / 折线图 / 可点击的交易列表 + Markdown 文字（支持表格）作答，答复流式逐字呈现；点结果里的交易可直接进详情页。用的是你在「AI 记账设置」里配置的服务，未配置时页面会引导去配置。聊天记录**仅存本机**、不进备份，右上角可一键清空。AI 全程只读，不会改动任何账目数据。

## [1.10.7] - 2026-07-11

### 优化
- 退款金额输入：数字键盘里直接显示「最多 XXX（剩余可退）」，超上限时点确认当场封顶——不用等保存才知道超了。
- 退款编辑弹窗的「删除」挪到右上角，与底部「保存」分开，避免误触。

## [1.10.6] - 2026-07-11

### 新增
- **退款升级为独立条目**：一笔支出可添加**多笔退款**，每笔各记**金额 / 到账账户 / 发起日期 / 到账日期 / 备注**，支持全额 / 部分退款（不超过原额，超出会提示）。在交易详情页的「退款」区添加、编辑、删除。
- **待到账 / 已到账两态**：退款可先记为「待到账」（已申请、钱还没回来，不影响余额与统计），到账后一键「标记已到账」核销。交易列表页头新增「**待退款**」清单，汇总所有在路上的退款、显示合计、可逐笔一键核销。
- 退款可**退到与原支付不同的账户**（如原卡已注销、平台退到余额）。

### 优化
- 退款回款不再计入收入、也不虚增花费：只有「已到账」才冲减对应支出的净额、并把钱加到到账账户，账户余额与收支统计更准确。旧版的单笔退款金额在升级后自动转成一条退款记录，数据无损。

## [1.10.5] - 2026-07-11

### 优化
- 账户详情页的设置项**分组展示**（基本信息 / 卡片信息 / 信用 / 展示与记账 / 危险操作），像设置页一样按模块分区，不同账户类型只显示相关模块，选项多时更清晰;删除账户入口标为红色。
- 账户详情里「卡号后四位」入口改名为「卡号」——点进去本就是填完整卡号 + 后四位,原名容易误解。

### 修复
- 信用卡 / 信用账户**还款后**,首页「最近交易」里那笔还款曾错误显示为「已删除分类」(点进去其实是转账)。现在还款按正常转账分类记录,列表显示正确。
- 用数字键盘设置**信用额度 / 预算**等可为 0 的金额时,按 **C 清空**后「确定」按钮点不了、必须先输个 0 才能确认。现在清空后即可直接确认为 0(用于清除额度 / 预算)。

## [1.10.4] - 2026-07-11

### 新增
- 新增**信用账户**账户类型（位于「网络支付」与「信用卡」之间），适配花呗 / 白条等有额度、账单日、还款但没有实体卡号的信用类账户。
- 信用卡 / 信用账户新增**信用额度**：在账户详情页设置总额度后，直观展示已用额度、可用额度与额度使用进度条；设了账单日的还展示**本期账单**（当前账单周期消费，附账单日 / 还款日提示）。资产列表里信用类账户也会显示「可用额度」。
- 信用卡 / 信用账户新增**还款**入口：账户详情页点「还款」即可发起还款，金额默认预填当前欠款、可改，扣款账户可自由选择、也可选「无账户（代还）」适配他人代还；还款按转账记账、不计入收支统计。
- 信用卡 / 储蓄卡新增**完整卡号**（选填）录入：可填完整卡号，账户详情页展示并支持一键复制，方便转账时粘贴。后四位默认「跟随完整卡号」自动取末四位，也可关掉开关手动填写；**开关状态会被保存并随备份导出 / 导入**，关掉后重新打开或换机导入都保持你的选择，不会又变回跟随；列表 / 首页仍只显示后四位。

## [1.10.2] - 2026-07-10

### 优化
- 收支统计页的走势图新增**周 / 月 / 季 / 年**视图切换：顶部可在四种时间跨度间切换查看支出/收入/转账走势（周·月·季按天描点，年按 12 个月描点），前后翻页按当前档位步进；年视图下方按月列出，点某月即可下钻回该月视图。此前只能按整月查看、切月后总回到当月。
- 预算设置页的「分类预算」改为**大类带小类的可折叠层级**（与分类管理页一致）：顶级分类为一行、显示已含子分类的合计花销/预算，点箭头展开设置各子分类预算。分类多时更好查找，也方便先设大类预算再细分小类。

### 修复
- 导入一木记账账单时，带**退款**的支出现在会正确按「金额 − 退款」记净额（退款金额回到原账户），此前「退款」这一列被忽略、支出记成了未扣退款的毛额。

## [1.10.1] - 2026-07-10

### 修复
- 桌面小组件跨天不刷新：此前「今日支出 / 本月可用预算」小组件在你不打开 App 时，次日仍停留在昨天的数据，必须打开一次应用才更新。原因是午夜自愈闹钟在手机夜间休眠（Doze）时被系统压制、整夜不触发。现改为可穿透休眠并唤醒设备的午夜闹钟，并加了「设备活跃时」的兜底刷新，跨天/跨月后无需打开 App 也会自动归零 / 回到整月预算。（国产 ROM 若把应用彻底清理后台仍可能失效，建议给应用加自启动 / 后台白名单。）
- 修复「统计分析 → 分类排行」里同一个分类（如「餐饮」）偶尔会分裂成两行、金额与笔数对不上，而分类管理里其实只有一个的问题。根因是历史 / 异构备份可能带入「指向已删除分类的交易」或「重复的同名分类」。现在：① 分类排行不再把这类异常引用冒名成同名分类，而是显示为「已删除分类」；② 应用启动 / 导入备份时会自动做一次分类数据自愈——把指向已删除分类的交易归入「未分类」、把重复的同名分类合并（交易一并迁移）、把父分类已丢失的子分类重挂为顶级；③ 新增分类唯一性约束，从源头防止再产生重复同名分类。

## [1.10.0] - 2026-07-09

### 安全
- 备份写入后会自动**回读并逐字节校验**，若文件写坏 / 被截断会立即提示（手动备份）或不记为成功、下次自动重试（自动备份），不再「以为备份成功、实际已损坏」。

## [1.9.2] - 2026-07-09

### 优化
- 周期记账编辑页选择账户,改用与「记一笔 / 编辑交易」相同的账户选择器:带账户图标、当前余额、卡号后四位;收支类型的转出账户也支持选择「无账户」,不再是一列无图标无余额的纯文字。
- 设置月度 / 分类 / 每日预算的金额输入,改用与记账一致的数字键盘(支持 `500+800` 这类算式),不再弹出系统文本输入框。
- 账户分组选择图标时,改为像账户图标一样可直接看到图标预览,不再是只有文字名称的列表。
- 统一各处「删除 / 清空 / 重置数据」确认框的确认按钮为红色,危险操作的视觉提示一致(此前删交易、删分类等的确认按钮是普通蓝色)。
- 交易列表按分类筛选,改用与记账一致的分类选择器(带图标、可折叠的父子层级),顶部提供「全部」项,不再是纯文字加空格缩进的列表。

### 修复
- 删除账户时,原本引用该账户的周期记账规则会被自动停用并清空其账户(改为「无账户」),并提示你前往复查,不再持续往已删除的账户补记流水。
- 分类若正被周期记账规则引用,将无法删除并给出提示,避免规则到期后生成「归不了类」的交易。
- 切换到后台时一并把刚记下的交易落盘(此前仅刷新偏好设置),消除极端情况下「刚记的账被系统回收丢失」的隐患。
- 数字键盘算式记账(如 `0.1+0.2`)结果规整到「分」,不再把 `0.30000000000000004` 这类浮点尾差存进账目。
- 导入 Tally 备份的预览页,账户「导入后余额」改为随你排除 / 修改交易实时重算,确保预览所见与实际导入结果一致(此前一直显示 Tally 原始余额,排除交易后会对不上)。
- 导入 Tally 备份时,同名的多个资产账户(如两个都叫「现金」的钱包)不再被合并成一个,而是各自建立、余额各归各的(重名的自动加「 (2)」后缀区分)。

## [1.9.1] - 2026-07-09

### 优化
- 分类图标选择器改为自适应网格,图标按可用宽度均匀铺满,不再出现右侧不均的留白。
- 「移动分类 / 合并分类」的目标选择改为带图标的层级树(和记账时选分类一致),可看清「餐饮 / 午餐」这类父子层级,不再是无图标的平铺列表。

## [1.9.0] - 2026-07-09

### 新增
- 统计分析页新增**子分类统计**:排行可在「分类 / 子分类」间切换,顶级分类模式下点击某个分类即可下钻查看它下面各子分类的金额/占比拆分。
- 统计分析页新增**标签统计**维度:可按标签查看某时间范围、支出/收入下的金额排行(一笔可带多个标签,占比之和可能超过 100%,页面已注明)。
- 分类图标支持自定义:内置图标从 17 个扩充到 60+,并新增 **emoji 图标**——在「分类管理 → 选择图标」里可从常用 emoji 快选,或自己输入任意 emoji 作为分类图标。
- 分类管理页新增「合并到其他分类」：点击某个分类即可把它的全部交易并入另一个同类型分类并删除原分类，用于统一同义分类（如把「交通出行」并入「交通」），合并后汇总更清爽（周期记账里的引用也会一并迁移）。

### 修复
- 修复浅色主题下「交易明细」列表页与交易详情页仍显示为深色、强光下看不清的问题，这两个页面现在跟随应用的浅色/深色设置。

## [1.8.14] - 2026-07-09

### 修复
- 修复导入只有账户、没有交易记录的账单（如 Tally 只导出资产账户）时提示「没有数据无法导入」的问题，现在这类文件也能进入预览并导入账户。

### 优化
- 导入预览页的「导入账户」区在来源带余额时默认展开，并在每个账户右侧显示导入后的余额，便于确认后再落库；纯账户导入时确认按钮显示为「确认导入（N 个账户）」。

## [1.8.13] - 2026-07-09

### 优化
- **Tally 导入现在会一并导入账户余额与类型**：不再只靠交易流水推算余额，而是按 Tally 里各资产账户的当前余额对齐导入后的账户余额；负债类账户记为负余额，借出/理财等记为正余额，「不计入总资产」的设置也会保留。没有任何流水的账户（如零余额钱包、借出/负债对象）也会一并创建，不再遗漏。

### 修复
- 修复桌面小组件跨天/跨月后仍显示旧数据的问题：「今日支出」在新的一天会自动归零、「本月可用预算」在新月会回到整月预算，无需打开应用即可自愈（约每日午夜刷新一次）。

## [1.8.12] - 2026-07-08

### 修复
- 修复「选择账单来源」弹窗在项目较多 / 屏幕较矮时内容溢出且无法上下滑动的问题，现可正常滚动查看全部来源。

## [1.8.11] - 2026-07-08

### 新增
- 账单导入新增 **Tally 记账** 来源：导入 Tally「备份」导出的 zip（内含 `backup_data.json`），保留精确到秒的交易时间，收入/支出/转账、一级/二级分类、账户与备注一并导入（分类按二级分类，转账账户从记录备注中解析）。

### 修复
- 修复极短时间内给同一交易连续添加多张图片附件时、个别附件可能共用同一内部 id，导致删除一张会误删多张的问题。

## [1.8.10] - 2026-07-08

### 优化
- 记账数字键盘布局调整：小数点移到 `0` 右边、`00` 移到 `0` 左边，「OK」键放大为竖向两格，更顺手。
- 移除数字键盘的正负号键；账户余额如需调整为负数，可用算式输入（如 `0-100`）。

## [1.8.8] - 2026-07-08

### 修复
- 修复 **1.8.7 的截图识账 / 分享图片识账在正式（release）安装包上直接报「识别失败」** 的问题：R8 混淆漏掉了默认优化规则、又缺少对 ML Kit 文字识别类的 keep，导致识别器初始化时崩溃（debug 版正常、release 版才炸）。分享文本 / 自动化文本入口不受影响。

## [1.8.7] - 2026-07-07

### 新增
- **截图识账**：在微信/支付宝等任意 App 里把账单**截图「分享」给 Veri Fin**，或在 AI 记账弹层里点「截图识账」从相册选图——文字识别在**本机离线完成、图片绝不上传**，识别出的文本交你自配的 AI 服务解析成草稿，确认后才落账。需先配置 AI 记账。因内置离线中文文字识别模型，安装包体积有所增大。
- **分享文本识账**：把账单文本（如银行通知原文）分享给 Veri Fin，同样解析成草稿确认落账。
- **自动化接入**：新增给 Tasker/MacroDroid 等自动化工具的显式 Intent 接口，可把抓到的账单文本送进 Veri Fin 解析（只产草稿弹确认，**绝不静默落账**），用法见 `docs/automation.md`。Veri Fin 本体**不监听任何通知或屏幕**。

## [1.8.6] - 2026-07-07

### 修复
- 修复**把一笔待报销支出的金额改到低于已报销/退款额**后，这笔支出会被错误计入「收入」、账户余额虚增的问题：净额现在钳制为不小于 0。
- 数据库因损坏或**降级安装了更旧版本**而打不开时，不再白屏卡死，改为显示明确的错误页——提示数据很可能仍在本机、**请勿清除应用数据**，并给出重装最新版的引导；绝不会自动删库。
- **恢复备份 / 重置数据 / 删除账本**现在在单个数据库事务内完成：万一中途进程被杀或写入出错，会整体回滚，不再可能留下「交易换了但账户没换」的半新半旧状态。
- WebDAV 请求增加连接与响应超时：当服务器无响应时不再无限期挂起，此前这会导致**自动备份此后被静默跳过、一直不再运行**（需重启应用才恢复）。
- 应用切到后台时刷盘挂起的设置写入，缩小「刚设完应用锁 / 刚同意隐私政策就被系统回收 → 设置丢失」的窗口。
- 导入账单时遇到不存在的日期（如 2 月 30 日）或越界时间，不再被静默改成邻近日期，而是按错误行提示、不误记。
- 备份 / 导入失败的提示改用本地化文案，不再向英文用户显示中文原文或原始技术异常信息（空文件、格式无效等有了清晰提示）。

## [1.8.5] - 2026-07-07

### 优化
- 收支统计页图表的 x 轴日期改为**每 5 天显示一个**（1、5、10……），不再把整月日期挤成一团；手指滑动时数据气泡仍显示具体某天。首页概览卡月末多日时同样稀疏显示。
- 收支统计页下方**每日记录可点击**，点进即某天的交易明细（与首页日历点日期一致）。
- 概览卡自定义页布局调整：**卡片标题移到顶部**、各分区用卡片包起来、点槽位的**指标选择列表按类别分组**（本月 / 今日 / 本周 / 本年 / 总额 / 资产 / 报销）加小标题。

### 修复
- 概览走势图数据气泡里「支出」金额误带正号（如「支出 +23.19」），改为负号「-23.19」。

## [1.8.4] - 2026-07-07

### 新增
- **导入预览新增「导入账户 / 分类」映射区**（可折叠）：账单里要新建的账户 / 分类会集中列出，可给每一个**改名**，或**映射到已有的账户 / 分类**——映射后所有引用它的交易一次性全部改过去，不用逐条改。

### 优化
- 导入预览的交易列表改为**按日期分组**（从新到旧、带当日合计），与交易列表样式一致。
- 导入预览顶部汇总卡与下方交易列表左右**对齐**，不再宽窄不一。

## [1.8.3] - 2026-07-07

### 新增
- **首页概览卡可自定义**：进入收支统计页后点右上角「编辑」，可分别设置卡片上大数字、结余位与三个小卡片各展示什么数据（本月 / 今日 / 本周 / 本年 / 总的收入·支出·结余、日均消费·收入、总资产 / 负资产 / 净资产、待报销 / 已报销等），可设置走势曲线画支出 / 收入 / 结余，还能自定义卡片标题（留空显示「概览」），并一键恢复默认。

### 修复
- 首页概览走势窗口改为「累积展开」：当月 1 号起累积展示，到 8 号显示 1–14、15 号显示 1–21…… 直到覆盖整月，不再每 7 天跳成一段孤立的区间。

## [1.8.2] - 2026-07-07

### 新增
- **导入账单新增「导入预览」**：选好文件后不再直接入账，而是先进入预览页，用交易卡片列出即将导入的每一笔。可逐条排除 / 恢复、点进完整编辑（改金额、分类、账户、日期、备注等），确认后才真正导入；只会创建被保留交易实际用到的新账户 / 分类。

## [1.8.1] - 2026-07-07

### 修复
- 账单来源选择弹窗中图标与文字未垂直对齐的问题。

## [1.8.0] - 2026-07-07

### 新增
- **支持「一木记账」账单导入**（Excel .xls）。一木把账单与转账分开导出，对应两个独立入口——「一木记账 · 账单」和「一木记账 · 转账还款」，各选对应文件即可；选错文件会明确提示，不会误导入。
- 一木转账完整保留转出账户、转入账户与手续费。

### 优化
- 账单来源选择弹窗换上真实软件图标（支付宝 / 微信 / 薄荷记账 / 一木记账），更易辨认。

## [1.7.3] - 2026-07-07

### 优化
- 数据管理页重新分组、加小标题；「导出 / 导入」并入「本地备份」卡片，布局更清晰。

## [1.7.2] - 2026-07-07

### 优化
- 设置页按「通用 / 记账 / 关于」分组并加小标题，更易查找。

## [1.7.1] - 2026-07-06

### 优化
- 点「立即备份」时显示「备份中」转圈，避免点击后无反馈。

## [1.7.0] - 2026-07-06

### 新增
- 新增软件日志，数据保存失败时给出提示，方便排查。
- 记账提醒权限被拒时会提示，避免提醒静默失效。

### 隐私与安全
- 启用应用锁后开启防截屏：余额不再出现在系统「最近任务」缩略图或截图中。
- 配置 http 公网端点（WebDAV / AI）时，警告明文凭证外发风险。

### 备份
- 导入账单、调整余额也会触发自动备份与桌面小组件刷新。
- 自动备份更省资源；WebDAV 按保留份数自动清理旧备份。

### 修复
- 导入时拒绝把加密备份误当空备份，杜绝误清空数据。
- 修复周期记账按月 / 按年推进时的月末日期漂移（如每月 31 号）。
- AI 识别遇到异常字段时安全降级，不再报错中断。
- 英文界面下 AI 报错等不再夹带中文。
- 修复一批小问题：同账户转账、日期区间逆序、Excel 日期在夏令时地区偏移、图片重复压缩等。

---

更早的版本（1.6.8 及之前）未在此记录。
