<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root" android:layout_width="match_parent" android:layout_height="match_parent">
    <!-- The launcher owns the cell size; the visible card stays a compact 72dp. -->
    <LinearLayout android:id="@android:id/background" android:clipToOutline="true"
        android:layout_width="match_parent" android:layout_height="72dp"
        android:layout_gravity="center_vertical" android:orientation="horizontal"
        android:gravity="center_vertical" android:paddingLeft="12dp" android:paddingRight="8dp"
        android:background="@drawable/widget_surface_dark">
        <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:orientation="vertical">
            <TextView android:id="@+id/widget_title" android:layout_width="match_parent"
                android:layout_height="wrap_content" android:text="@string/widget_today_expense"
                android:textColor="#FFB8C0CC" android:textSize="11sp" android:includeFontPadding="false"
                android:maxLines="1" android:ellipsize="end"/>
            <TextView android:id="@+id/widget_label" android:layout_width="wrap_content"
                android:layout_height="wrap_content" android:visibility="gone"/>
            <TextView android:id="@+id/widget_amount" android:layout_width="match_parent"
                android:layout_height="28dp" android:layout_marginTop="4dp" android:text="0"
                android:textColor="#FFF5F5F7" android:textSize="24sp" android:textStyle="bold"
                android:autoSizeTextType="uniform" android:autoSizeMinTextSize="14sp"
                android:autoSizeMaxTextSize="24sp" android:maxLines="1" android:includeFontPadding="false"/>
        </LinearLayout>
        <TextView android:id="@+id/widget_add_button" android:layout_width="48dp"
            android:layout_height="48dp" android:layout_marginLeft="8dp" android:text="+"
            android:textColor="#FFFFFFFF" android:textSize="26sp" android:gravity="center"
            android:includeFontPadding="false" android:background="@drawable/widget_quick_action"
            android:contentDescription="@string/quick_entry_button"/>
    </LinearLayout>
</FrameLayout>
